//Logar cada um dos elementos de array com seu valor dobrado

var numeros = [1, 5, 3];

var numeros2 = numeros.map(n => n*2);

for (var i = 0; i < numeros2.length; i++){
  console.log(numeros2[i])
}

